use Piece;
use Color::Color;
use GameState::{GameState, Space};

pub fn get_pawn_moves (from: Space, state: GameState) {
    if form.0 != GameState.to_act.seventh_rank() {
        let to = from.offset(rank_offset: i32, file_offset: i32)
        //Standard move
        //Capture
    } else {
        //Promotion Standard
        //Enpassant
        //Promotion Capture        
    }
    if 
    
    
   

    //Double move
    //Enpassant
}

pub fn get_casteling_moves


